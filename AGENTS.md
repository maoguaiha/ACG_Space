### 1. 技术栈约束 (不得偏离)

- **后端**：Java 17 + Spring Boot 3 + MyBatis-Plus。
- **JSON**：必须用 **Fastjson2**。
- **管理端**：Vue 3 (Setup 语法) + Element Plus + Pinia。
- **用户端**：Nuxt 3 + Tailwind CSS。

### 2. 开发底线 (强制执行)

- **Bean 安全**：定义 Bean（尤其是 Redis 配置）必须显式命名，严禁出现 `BeanDefinitionOverrideException` 冲突。
- **异步幂等**：所有 RocketMQ 消费者必须包含**幂等性校验**逻辑。
- **校验先行**：接口入参必须用 `@Validated`，严禁裸奔。
- **环境隔离**：密钥、密码禁止硬编码，统一写在 `.env` 或 `yml` 配置文件中。
- **非必要不修改**：尽量不动跟本模块不强相关的代码。如果不确定是否"强相关"，先问。

### 3. AI 协作逻辑
- **不懂就问**：面对需求不明显的地方要问我。但不是什么都问我，不影响的情况下自己解决。只要新增加依赖或修改接口签名不应该影响其他功能正常使用就行。
- **先计划后动手**：修改 **5 个文件以上**时，必须先列出计划（Plan）。
- **TypeScript**：前端代码必须强制使用 TypeScript，禁止使用 `any`。
- **有自由度**：单一模块的实现确定计划后，在不影响最后成效的情况下自己决定实现顺序

### 4. 文档更新

- **及时更新文档**：完成一系列功能后，如果我直奔下一个主题，你要询问我要不要更新 document 下的 develop 目录下的需求、功能迭代文档，以便后续开发和更新。
- **自我学习避免重复错误**：将自己犯的错和疏忽导致的bug，要精简表达放入 `document/study/study.md` 文件中，及时更新。
- **自动缩短上下文**：每次回复的时候精简总结上一条消息的重点，将上条消息的重点覆写到 `document/point/point.md` 文件中不断迭代。每次发消息时，应该先阅读这个文件，尽量不阅读之前的聊天记录；如果要阅读之前的聊天记录，先提前问我不知道的东西。

### 5. point 文件使用规则

**位置**：`document/point/point.md`

**内容格式**：
```markdown
# 上下文重点 - point

## 项目概述
（项目简介和技术栈）

## 规则要点
（关键开发规则）

## 当前进度
（正在进行的任务）

## 待完成
（下一步计划）
```

**更新时机**：
- 每完成一个重要功能后更新
- 每解决一个 bug 后更新
- 每次开始新任务前先阅读再开始
